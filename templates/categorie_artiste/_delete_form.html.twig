<form method="post" action="{{ path('categorie_artiste_delete', {'id': categorie_artiste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_artiste.id) }}">
    <button class="btn">Delete</button>
</form>
