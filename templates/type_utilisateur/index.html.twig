{% extends 'base.html.twig' %}

{% block title %}TypeUtilisateur index{% endblock %}

{% block body %}
    <h1>TypeUtilisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomType</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_utilisateur in type_utilisateurs %}
            <tr>
                <td>{{ type_utilisateur.id }}</td>
                <td>{{ type_utilisateur.nomType }}</td>
                <td>
                    <a href="{{ path('type_utilisateur_show', {'id': type_utilisateur.id}) }}">show</a>
                    <a href="{{ path('type_utilisateur_edit', {'id': type_utilisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_utilisateur_new') }}">Create new</a>
{% endblock %}
