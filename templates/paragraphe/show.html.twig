{% extends 'base.html.twig' %}

{% block title %}Paragraphe{% endblock %}

{% block body %}
    <h1>Paragraphe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paragraphe.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ paragraphe.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paragraphe_index') }}">back to list</a>

    <a href="{{ path('paragraphe_edit', {'id': paragraphe.id}) }}">edit</a>

    {{ include('paragraphe/_delete_form.html.twig') }}
{% endblock %}
