{% extends 'base.html.twig' %}

{% block title %}PhotoParagraphe{% endblock %}

{% block body %}
    <h1>PhotoParagraphe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo_paragraphe.id }}</td>
            </tr>
            <tr>
                <th>Photographie</th>
                <td>{{ photo_paragraphe.photographie }}</td>
            </tr>
            <tr>
                <th>Miniature</th>
                <td>{{ photo_paragraphe.miniature }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photo_paragraphe_index') }}">back to list</a>

    <a href="{{ path('photo_paragraphe_edit', {'id': photo_paragraphe.id}) }}">edit</a>

    {{ include('photo_paragraphe/_delete_form.html.twig') }}
{% endblock %}
