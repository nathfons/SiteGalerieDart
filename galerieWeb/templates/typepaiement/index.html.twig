{% extends 'base.html.twig' %}

{% block title %}Typepaiement index{% endblock %}

{% block body %}
    <h1>Typepaiement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nomtypepaiement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typepaiement in typepaiements %}
            <tr>
                <td>{{ typepaiement.id }}</td>
                <td>{{ typepaiement.nomtypepaiement }}</td>
                <td>
                    <a href="{{ path('typepaiement_show', {'id': typepaiement.id}) }}">show</a>
                    <a href="{{ path('typepaiement_edit', {'id': typepaiement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('typepaiement_new') }}">Create new</a>
{% endblock %}
