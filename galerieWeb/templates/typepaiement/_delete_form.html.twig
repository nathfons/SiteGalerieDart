<form method="post" action="{{ path('typepaiement_delete', {'id': typepaiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typepaiement.id) }}">
    <button class="btn">Delete</button>
</form>
