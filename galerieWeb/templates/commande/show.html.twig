{% extends 'basemenu.html.twig' %}

{% block title %}Commande{% endblock %}

{% block contentPage %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Referencecommande</th>
                <td>{{ commande.referencecommande }}</td>
            </tr>
            <tr>
                <th>Etatcommande</th>
                <td>{{ commande.etatcommande }}</td>
            </tr>
            <tr>
                <th>Datecommande</th>
                <td>{{ commande.datecommande ? commande.datecommande|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Datelivraison</th>
                <td>{{ commande.datelivraison ? commande.datelivraison|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_index') }}">back to list</a>

    <a href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
