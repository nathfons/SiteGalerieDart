<form method="post" action="{{ path('photographie_artiste_delete', {'id': photographie_artiste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photographie_artiste.id) }}">
    <button class="btn">Delete</button>
</form>
