{% block contentPage %}
    <h3>Choix d'artiste à la une</h3>

     <table class="table">
        <thead>
            <tr>
                <th>No</th>       
                <th>Artiste</th>
                <th>Photo</th>
                       
                <th>A la une</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
       
        {% for artiste in artistesAlaune %}
            <tr>
                <td>{{ loop.index0+1 }}</td>
                
                <td>{{artiste.prenom}} {{artiste.nom}}</td>
                <td><img align="center" class="img-fluid rounded" src="{{asset(artiste.miniature)  }}" alt="Image de {{ artiste.nom }}"></td>
                
                <td>{{artiste.alaune ? 'Yes' : 'No'}}</td>
                
               
               {% if artiste.alaune!= '1' %}
                 <td>
                <form action ="{{ path('approuver_alaune') }}" method="POST"> 
                    
	                <input type="checkbox" name="approuver_alaune_name" value = {{artiste.id}}> 
	                <button type="submit">APROUVER</button>
                </form>
                </td>   
                {% else %}
                <td></td>
                {% endif %}
               
            </tr>
           
            
        {% else %}
            <tr>
                <td colspan="19">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   
</form>

{% endblock %}
